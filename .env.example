# Stalwart instance base URL — no /api suffix needed
STALWART_URL=https://stalwart.example.com

# HTTP server port
PORT=3000

# Where request/response logs are written. Defaults to ./logs next to the app.
# Point this at a writable volume when the container runs as a non-root user.
LOG_DIR=/var/log/staliaswarden
